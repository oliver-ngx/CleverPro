"""
The Configs domain: what a change to a source file is, and what applying one
means.

Empty until Configs writes its first file. What is expected here is the bridge's
half of the work — resolving a source location to a node, mutating one property,
printing the file back — and it is expected to be pure, so that it can be
exercised without a browser or a dev server anywhere near it.
"""
