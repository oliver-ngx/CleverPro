# Runtime dependencies, pinned.
#
# This list used to carry anthropic, SQLAlchemy, python-dotenv and
# annotated-doc, none of which any module imported. They were dropped: an
# unused dependency is install time, image size and supply-chain surface spent
# on nothing. If persistence arrives, SQLAlchemy comes back with the code that
# uses it.
fastapi==0.141.1
pydantic==2.13.4
pydantic_core==2.46.4
starlette==1.6.0
uvicorn==0.52.1

# Multipart form parsing, which FastAPI requires for the UploadFile on
# POST /import. Imported by the framework rather than by this code.
python-multipart==0.0.32
