# Runtime dependencies, pinned, and matching back-end's versions so the two
# services cannot drift into needing two different FastAPIs.
#
# Only what the health route actually needs. The bridge will want a way to read
# and write source files and to hold a socket open; those arrive with the code
# that uses them, not before.
fastapi==0.141.1
pydantic==2.13.4
pydantic_core==2.46.4
starlette==1.6.0
uvicorn==0.52.1
